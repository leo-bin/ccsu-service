<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hang.dao.StudentDAO">

    <insert id="insert">
        insert into
        student_info(jwc_account, open_id, quality_fraction, comprehensive_fraction, grade, nick_name, department)
        values(#{jwcAccount}, #{openId}, #{qualityFraction}, #{comprehensiveFraction}, #{grade}, #{nickName}, #{department})
    </insert>

    <update id="update">
        update student_info
        <set>
            <if test="nickName != null">
                nick_name = #{nickName},
            </if>

            <if test="grade != null">
                grade = #{grade},
            </if>

            <if test="department != null">
                department = #{department},
            </if>

            <if test="jwcAccount != null">
                jwc_account = #{jwcAccount}
            </if>
        </set>
        where open_id = #{openId}
    </update>


    <select id="selectStudentDOByOpenId" resultType="com.hang.pojo.data.StudentDO">
        select * from student_info where open_id = #{openId}
    </select>

</mapper>